using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// This code is best of the best
public class Destroy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Destroy(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
